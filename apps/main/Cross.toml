# Cross 交叉编译配置文件
# 用于在 Ubuntu 上交叉编译 ARM64 版本

[target.aarch64-unknown-linux-gnu]
# 为交叉编译设置 OpenSSL
# git2 的 vendored-openssl 特性应该会自动处理，但这里确保环境变量正确
pre-build = [
  "echo 'Using vendored OpenSSL for cross-compilation'"
]

