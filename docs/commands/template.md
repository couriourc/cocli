# 模板管理命令

## qcl template list

列出所有可用的模板。

### 语法

```bash
qcl template list
```

### 示例

```bash
qcl template list
```

### 输出示例

```
可用的模板:
  - react
  - vue2
  - vue3
```

### 说明

- 从所有配置的仓库中搜索模板
- 显示所有可用模板的名称
- 模板定义在仓库的 `meta.yaml` 文件中

