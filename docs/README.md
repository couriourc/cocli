# QCli 文档

这是 QCli 的文档站点，使用 VitePress 构建。

## 开发

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm docs:dev
```

## 构建

```bash
# 构建文档
pnpm docs:build

# 预览构建结果
pnpm docs:preview
```

## 部署

构建后的文件在 `docs/.vitepress/dist` 目录，可以部署到任何静态网站托管服务。

